import { UserProfile } from "@clerk/nextjs";

const ProfilePage = () => {
  // Just realizing hoe redundant this page is to the grand scgeme of the app.
  return <UserProfile></UserProfile>;
};

export default ProfilePage;
